# Datasets / modelos / outputs
# Trailing-slash variants ignored dirs; bare names also cover symlinks in worktrees
data
data/
datasets
datasets/
checkpoints
checkpoints/
outputs
outputs/
runs
runs/
wandb
wandb/
models
models/
results
results/
pondernet/results/
pondernet/features/

# Worktree-local scripts y configs
activate.sh
.claude-config/
.gh-config/

# Entornos virtuales
.venv/

# Python
__pycache__/
*.pyc

# Secrets
.env

# Session scratch / handoff notes (local only)
last-session-AGENTS.md

# Superpowers plans / internal tooling (local only); matches at any depth
superpowers/

# macOS
.DS_Store

# MLflow tracking (reserved for future use)
mlruns/

# Eval splits tracked in git so the A100 hand-off is self-contained
# (negations must re-include each parent level of an ignored dir)
!data/
data/*
!data/gsm8k_aug/
data/gsm8k_aug/*
!data/gsm8k_aug/test.jsonl
!data/gsm8k_aug/validation.jsonl
