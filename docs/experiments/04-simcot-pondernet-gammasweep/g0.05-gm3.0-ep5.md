# g0.05-gm3.0-ep5

**Experiment:** [04-simcot-pondernet-gammasweep](runs.md)  **Date:** 2026-06-16  **Status:** ✅ done

> ✅ **Re-validated 2026-06-23.** Metrics below are on the held-out validation split (500 ex, greedy); prior test-set numbers were optimistically biased. See [eval-split note](../../experiments.md#eval-split-and-leakage-note).

## Summary
The sweet spot of the sweep: re-validated at **41.40% @ thr0.8 / 5.340 avg_steps** and **40.80% @ thr0.5 / 4.060 avg_steps** (validation, n=500). Near the 40.80% greedy validation baseline while spending ~30% fewer steps at thr0.5 - the value is step-efficiency, not an accuracy win. Only this run's ep3 (`checkpoint-2334`) survived; the other 7 γ configs and this run's other epochs were **deleted**, so the γ-sweep winner selection (done on the test set) **cannot be reproduced on validation**.

## Hyperparameters   _(from `command.sh`)_
| param | value |
|-------|-------|
| method | PonderNet adaptive halting |
| epochs · lr · eff. batch | 5 · 2e-5 · 128 |
| γ · geom_mean · K_max | 0.05 · 3.0 · 6 |
| warm-start · seed · data | full-model · 42 · train100k.jsonl |

## Results - re-validated (validation split, n=500, greedy)

Only `checkpoint-2334` (ep3) survived re-validation.

| checkpoint | thr 0.5 | thr 0.8 | thr 0.9 |
|-----------|---------|---------|---------|
| checkpoint-2334 (ep3) | 40.80% / 4.060 | 41.40% / 5.340 | 40.20% / 5.774 |

(acc% / avg_steps; n=500 validation; prior numbers were on GSM8K test, 1319 ex, biased.)

**Difficulty tracking** - Spearman(n_expr, steps_used), validation: thr0.5 = **+0.553**, thr0.8 = +0.524, thr0.9 = +0.414.

**Prior test-set numbers (biased):** thr0.8 40.18%, thr0.5 38.97%.

## Artifacts
- checkpoint: `models/checkpoints/04-simcot-pondernet-gammasweep/g0.05-gm3.0-ep5/`
- command + log: `outputs/04-simcot-pondernet-gammasweep/g0.05-gm3.0-ep5/{command.sh,train.log}`
- eval results: `results/04-simcot-pondernet-gammasweep/g0.05-gm3.0-ep5/`

## Notes
Best accuracy/efficiency trade-off; the recommended operating point. The accuracy reads
slightly above the 40.80% greedy validation baseline at thr0.8 (41.40%) but this is within
noise (n=500); the durable value is the step-efficiency at fixed accuracy. The γ-sweep
winner-selection was originally done on the **test** set (biased) and **cannot be reproduced
on validation**: the other 7 γ configs and this run's non-ep3 epochs were deleted.
